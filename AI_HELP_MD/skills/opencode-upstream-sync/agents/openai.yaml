interface:
  display_name: "OpenCode Upstream Sync"
  short_description: "Sync upstream, preserve Windows features, and log conflicts."
  default_prompt: "Use $opencode-upstream-sync to pull the latest work/windows-client-ui branch, merge official OpenCode dev while preserving local Windows UI features, update the single mandatory changelog, validate portable VSCode F5 startup, and push code plus records."
